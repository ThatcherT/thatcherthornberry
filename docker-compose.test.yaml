version: "3"

volumes:
  pgdata:
  redisdata:
  static_spotifly:

services:
  nginx:
    restart: always
    image: nginx:1.15-alpine
    depends_on:
      - spotifly
    volumes:
      - ./nginx:/etc/nginx/conf.d # this is how we use custom nginx build
      - static_spotifly:/app/spotifly/staticfiles
    ports:
      - "80:80"

  spotifly:
    restart: always
    build: .
    image: thatcherthornberry/spotifly:latest
    command: bash -c "python manage.py migrate && gunicorn music.wsgi:application -b 0.0.0.0:8000"
    expose:
      - 8000
    volumes:
      - static_spotifly:/app/spotifly/staticfiles
    env_file:
      - .env
    environment:
      - DOCKER_LOCAL=True
    depends_on:
      - postgres
  postgres:
    restart: always
    image: postgres:12.0-alpine
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - 5432:5432
    env_file:
      - .env
    environment:
      - POSTGRES_HOST=postgres
  redis:
    restart: always
    image: redis:6
    command: >
      --requirepass ${REDIS_PASSWORD}
    ports:
      - 6379:6379
    volumes:
      - redisdata:/data
  # TODO: refactor these to their own repo or something
  scheduler:
    restart: always
    image: thatcherthornberry/spotifly:latest
    command: python manage.py rqscheduler --queue default
    env_file:
      - .env
    environment:
      - DOCKER_LOCAL=True
    depends_on:
      - "redis"

  # TODO: refactor these to their own repo or something
  worker:
    restart: always
    image: thatcherthornberry/spotifly:latest
    command: python manage.py rqworker default
    env_file:
      - .env
    environment:
      - DOCKER_LOCAL=True
    depends_on:
      - "redis"
