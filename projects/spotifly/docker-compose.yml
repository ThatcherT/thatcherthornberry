version: "3"

volumes:
  pgdata:

services:
#  nginx:
#    image: nginx:1.15-alpine
#    ports:
#      - "80:80"
#      - "443:443"
#    depends_on:
#      - web
#    volumes:
#      - ./staticfiles:/home/app/staticfiles
#      - ./data/certbot/conf:/etc/letsencrypt # two shared volumes with certbot for validation
#      - ./data/certbot/www:/var/www/certbot
#      - ./data/nginx:/etc/nginx/conf.d # this is how we use custom nginx build

#  certbot:
#    image: certbot/certbot
#    volumes:
#      - ./data/certbot/conf:/etc/letsencrypt
#      - ./data/certbot/www:/var/www/certbot

#  postgres:
#    restart: always
#    image: postgres:12.0-alpine
#    volumes:
#      - pgdata:/var/lib/postgresql/data
#    ports:
#      - 5432:5432
#    env_file:
#      - .env

  spotiflyweb:
    restart: always
    build: .
    image: thatcherthornberry/spotifly:latest
    command: gunicorn music.wsgi:application --bind 0.0.0.0:8001
    expose:
      - "8001"
    env_file:
      - .env
#    depends_on:
#      - postgres

networks:
  default:
    external: true
    name: thatcherthornberry
